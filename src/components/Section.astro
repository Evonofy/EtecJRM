---
export interface Props {
  background: string
}

const { background } = Astro.props
---

<section class:list={[background ?? "bg-slate-50", "w-full p-6 lg:py-12"]}>
  <div class="m-auto flex max-w-5xl flex-col items-start gap-8 lg:gap-16">
    <slot />
  </div>
</section>
