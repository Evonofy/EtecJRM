---
export interface Props {
  background: string
}

const { background } = Astro.props
---

<section class:list={[background ?? "bg-slate-50", "w-full p-6"]}>
  <div class="flex flex-col items-start gap-8">
    <slot />
  </div>
</section>
